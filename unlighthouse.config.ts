/// <reference types="unlighthouse" />

export default {
	site: 'cinnamon.agency',
	debug: true,
	ci: {
		buildStatic: true
	},
	// dynamicSampling: false,
	// scanner: {
  //   // scan each URL 3 times and average the results
  //   samples: 3
  // },
	// puppeteerClusterOptions: {
  //     // only run 1 worker at a time 
  //   maxConcurrency: 1
  // }
}
